// import { Request, Response } from "express";
// import * as rejectedService from "../services/subBatchRejected";

// export const createRejected = async (req: Request, res: Response) => {
//   try {
//     const result = await rejectedService.createRejectedSubBatch(req.body);
//     res.status(201).json(result);
//   } catch (err: any) {
//     res.status(500).json({ error: err.message });
//   }
// };


